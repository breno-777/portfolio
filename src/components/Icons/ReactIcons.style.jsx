import { styled } from "styled-components";

export const Styled__Icons = styled.div`
    color: #EEEEEE;
    transition: transform 0.5s, color 0.5s;

    &:hover{
        transform: scale(1.1);
        color: #83ecfced;
    }
`;